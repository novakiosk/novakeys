name: Release NOVA Keys

on:
  push:
    paths:
      - Cargo.toml
      - Cargo.lock
      - .github/workflows/release.yml
      - .github/scripts/**
      - .github/licenses/**
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: novakeys-release-${{ github.sha }}
  cancel-in-progress: false

jobs:
  build:
    if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    outputs:
      build: ${{ steps.version.outputs.build }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Check whether this version needs a release
        id: version
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 .github/scripts/release.py check
      - name: Build and package in current stable Fedora
        if: steps.version.outputs.build == 'true'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          docker pull fedora:latest
          image=$(docker image inspect fedora:latest --format '{{index .RepoDigests 0}}')
          docker run --rm -v "$GITHUB_WORKSPACE:/source" -w /source \
            -e VERSION -e GITHUB_SHA -e FEDORA_IMAGE="$image" "$image" \
            bash .github/scripts/build-release.sh
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: steps.version.outputs.build == 'true'
        with:
          name: release-files
          path: dist/
          if-no-files-found: error
          retention-days: 7

  publish:
    needs: build
    concurrency:
      group: novakeys-publish-${{ needs.build.outputs.version }}
      cancel-in-progress: false
    if: needs.build.outputs.build == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          sparse-checkout: |
            .github/scripts/release.py
            Cargo.toml
            Cargo.lock
          sparse-checkout-cone-mode: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-files
          path: dist
      - name: Upload draft assets and publish
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 .github/scripts/release.py publish
